require('@babel/register');

const { resolve } = require('path');

module.exports = {
  config: resolve('src', 'config', 'db.js'),
  'models-path': resolve('src', 'models'),
  'migrations-path': resolve('src', 'database', 'migrations'),
  'seeders-path': resolve('src', 'database', 'seeders'),
};
